нужна презентация проекта по ИБ ВсОШ. сделай неперегруженные слайды, чтобы мне было что говорить. ниже описание слайдов и самого проекта 
🔹 СЛАЙД 1. Актуальность и контекст

О чём слайд (кратко на слайде):

Рост объёмов сетевого трафика

Большинство атак видно в трафике

Анализ трафика — основа сетевой форензики

Что говорить:

Сейчас практически все инциденты информационной безопасности так или иначе отражаются в сетевом трафике.
При этом объёмы трафика постоянно растут, и его анализ становится всё более сложной задачей, особенно для начинающих специалистов.
Поэтому тема анализа сетевого трафика и сетевой форензики является актуальной.

🔹 СЛАЙД 2. Проблема

На слайде:

PCAP-файлы → сотни тысяч пакетов

Ручной анализ сложен и долгий

Профессиональные инструменты сложны для новичков

Что говорить:

Основная проблема заключается в том, что PCAP-файлы могут содержать огромное количество пакетов.
Анализировать их вручную очень сложно, а существующие профессиональные инструменты ориентированы на опытных специалистов.
В результате новичкам трудно понять, где именно в трафике может скрываться проблема.

🔹 СЛАЙД 3. Цель и задачи проекта

На слайде:

Цель: упростить анализ трафика

Автоматическое выявление аномалий

Использование ИИ

Что говорить:

Цель моего проекта — разработать систему анализа сетевого трафика, которая автоматически выявляет аномалии и упрощает первичный анализ.
Для этого я реализовал анализ PCAP-файлов, извлечение признаков и использование обученных моделей искусственного интеллекта.

🔹 СЛАЙД 4. Сетевая форензика (коротко)

На слайде:

Анализ трафика для расследования инцидентов

Основной формат — PCAP

Что говорить:

Сетевая форензика занимается анализом сетевого трафика с целью выявления атак и расследования инцидентов.
Основным источником данных являются PCAP-файлы — дампы сетевых пакетов.
Именно с такими файлами работает мой проект.

🔹 СЛАЙД 5. Аналоги и ограничения

На слайде:

Wireshark — сложно, вручную

Zeek, Snort — сложная настройка

Нет фокуса на обучение

Что говорить:

Я рассмотрел существующие инструменты анализа трафика.
Они мощные, но сложные и плохо подходят для обучения.
Поэтому возникла идея создать инструмент, ориентированный именно на новичков и учебные задачи.

🔹 СЛАЙД 6. Концепция Traffic Analyzer

На слайде:

Автоматизация анализа

Фокус на потоках

Поддержка обучения

Что говорить:

Идея проекта — создать помощника для анализа сетевого трафика.
Система автоматически обрабатывает большие PCAP-файлы и показывает пользователю, какие сетевые потоки выглядят подозрительно.
Это позволяет сосредоточиться на анализе, а не на рутине.

🔹 СЛАЙД 7. Использование ИИ

На слайде:

Извлечение признаков

Обученные мной модели ИИ

Обнаружение аномалий

Что говорить:

В проекте используются обученные мной модели искусственного интеллекта.
Я извлекаю признаки сетевых потоков и на их основе обучаю модели машинного обучения.
Эти модели автоматически выявляют аномалии и подозрительное поведение в трафике.

🔹 СЛАЙД 8. Архитектура и алгоритм

На слайде:

Загрузка PCAP

Анализ и признаки

ИИ-анализ

Визуализация

Что говорить:

Работа системы начинается с загрузки PCAP-файла.
Затем анализируются пакеты и формируются потоки, извлекаются признаки, после чего применяется модель ИИ.
Результаты отображаются в виде отчётов и визуализаций.

🔹 СЛАЙД 9. Опрос целевой аудитории

На слайде:

Кто участвовал

Основные вопросы

Диаграммы

Что говорить:

Для подтверждения актуальности проблемы я провёл опрос целевой аудитории — обучающихся и начинающих специалистов.
На слайде представлены диаграммы с результатами опроса, которые показывают, что большинству сложно анализировать сетевой трафик вручную.

🔹 СЛАЙД 10. Применение и ценность проекта

На слайде:

Обучение форензике

Учебные лаборатории

Task-based CTF

Первичный анализ инцидентов

Что говорить:

Разработанная система может использоваться в образовательных целях, учебных лабораториях и при подготовке к CTF.
Она упрощает анализ сетевого трафика и помогает новичкам быстрее освоить основы сетевой форензики.








# Описание файлов проекта Traffic Analyzer

## Обзор проекта

Проект представляет собой систему анализа сетевого трафика из PCAP файлов с обнаружением аномалий с помощью машинного обучения и веб-дашбордом для визуализации результатов.

---

## Основные файлы приложения

### 1. `analyze_pcap.py` (корень проекта)

**Назначение:** Главная точка входа для анализа PCAP файлов из командной строки.

* Принимает путь к PCAP файлу
* Опционально принимает путь к обученной ML модели
* Запускает анализ через `traffic_analyzer.analyze_traffic()`
* Может автоматически запускать веб-дашборд после анализа
* Выводит статистику в консоль

**Использование:**

```bash
python analyze_pcap.py file.pcap [--model model.pkl] [--dashboard]
```

---

### 2. `dashboard/app.py`

**Назначение:** Flask веб-приложение для загрузки и просмотра результатов анализа трафика.

* **Роуты:**

  * `/` — список загруженных файлов
  * `/upload` — загрузка нового PCAP файла (POST)
  * `/analysis/<upload_id>` — просмотр результатов анализа
  * `/api/data/<upload_id>` — API для получения агрегированных данных с фильтрами
  * `/api/packets/<upload_id>` — API для получения списка пакетов
  * `/delete/<upload_id>` — удаление анализа
* Автоматически запускает анализ при загрузке файла
* Сохраняет результаты в `data/analyses/<upload_id>/`
* Отображает статистику, таблицы потоков и HTTP-запросы

---

### 3. `dashboard/templates/index.html`

**Назначение:** HTML-шаблон страницы просмотра результатов анализа.

* Общая статистика (количество потоков, аномалий, процент аномалий)
* Таблица потоков с цветовой индикацией уровня критичности
* Фильтры по уровню критичности, IP-адресу и времени
* Топ подозрительных IP-адресов
* График распределения по типам аномалий (Plotly)
* Таблица HTTP-запросов
* Модальное окно с деталями пакета или потока

---

### 4. `dashboard/templates/uploads.html`

**Назначение:** HTML-шаблон страницы загрузки PCAP файлов.

* Drag & drop зона загрузки
* Список всех загруженных файлов с метаданными
* Статистика по каждому анализу (потоки, аномалии, критичные события)
* Кнопки просмотра и удаления анализов
* Индикатор прогресса загрузки

---

## Модули анализа (`src/`)

### 5. `src/traffic_analyzer.py`

**Назначение:** Основной модуль анализа сетевого трафика.

* **Функции:**

  * `analyze_traffic()` — главная функция анализа PCAP файла
  * `_extract_flow_metadata()` — извлечение метаданных потоков (IP, порты, время)
  * `_extract_packets()` — формирование списка пакетов для дашборда
  * `_parse_http_request()` — парсинг HTTP-запросов из payload
  * `_determine_alert_level()` — определение уровня критичности аномалии
  * `_generate_report()` — генерация итогового отчета и статистики
* Интеграция с `feature_extractor` и `anomaly_detector`
* Сохранение результатов в CSV и JSON
* Поддержка эвристического анализа при отсутствии ML-модели

---

### 6. `src/feature_extractor.py`

**Назначение:** Извлечение признаков из PCAP файлов для последующего ML-анализа.

* **Функции:**

  * `extract_features_from_pcap()` — основная функция извлечения признаков
  * `_normalize_flow_key()` — нормализация ключей потоков (двунаправленные потоки)
  * `_get_protocol_name()` — определение протокола (TCP / UDP / ICMP)
  * `_get_ports()` — извлечение портов источника и назначения
* **Признаки:**

  * Количество пакетов и байт в потоке
  * Длительность и средняя скорость
  * TCP-флаги (SYN, ACK, FIN, RST)
  * Протокол и номера портов
  * Количество уникальных IP-адресов
* Сохранение результатов в CSV

---

### 7. `src/anomaly_detector.py`

**Назначение:** Модуль обнаружения аномалий на основе машинного обучения.

* **Класс `AnomalyDetector`:**

  * `train_model()` — обучение модели на нормальном трафике
  * `predict()` — классификация потоков (аномалия / норма)
  * `predict_anomaly_scores()` — расчет anomaly score
  * `save_model()` / `load_model()` — сохранение и загрузка модели
* **Функция `evaluate_model()`** — оценка качества (accuracy, precision, recall, F1, ROC-AUC)
* Использует Isolation Forest (scikit-learn)

---

### 8. `src/generate_test_traffic.py`

**Назначение:** Генерация тестовых PCAP файлов.

* Генерация нормального трафика (HTTP, HTTPS, DNS)
* Генерация аномального трафика:

  * Порт-сканирование
  * Высокочастотный трафик (DDoS-подобный)
  * Короткие TCP-сессии с RST
  * Нетипичные размеры пакетов и комбинации портов
* Использует `scapy`
* Применяется для тестирования и обучения моделей

---

## Конфигурация и инфраструктура

### 9. `requirements.txt`

**Назначение:** Python-зависимости проекта.

* flask
* pandas
* scapy
* scikit-learn
* plotly
* matplotlib

---

### 10. `Dockerfile`

**Назначение:** Docker-образ для запуска Traffic Analyzer.

* Базовый образ: `python:3.10-slim`
* Установка зависимостей
* Запуск веб-дашборда

---

### 11. `docker-compose.yml`

**Назначение:** Упрощенный запуск приложения.

* Сервис на порту 5000
* Монтирование директории `data/`

---

## Документация

### 12. `README.md`

**Назначение:** Краткое описание Traffic Analyzer и его возможностей.

### 13. `HOW_TO_USE.md`

**Назначение:** Подробная инструкция по запуску, анализу PCAP файлов и обучению модели.

---

## Структура данных

### 14. `data/`

**Назначение:** Хранилище данных анализа.

* `data/uploads/` — временное хранение загруженных PCAP файлов
* `data/analyses/<upload_id>/` — результаты анализа:

  * `analysis_results.csv`
  * `packets.json`
  * оригинальный PCAP файл
* `data/uploads_index.json` — индекс всех загрузок

---

## Итоговая структура проекта

```
traffic-analyzer/
├── analyze_pcap.py
├── dashboard/
│   ├── app.py
│   └── templates/
│       ├── index.html
│       └── uploads.html
├── src/
│   ├── traffic_analyzer.py
│   ├── feature_extractor.py
│   ├── anomaly_detector.py
│   └── generate_test_traffic.py
├── data/
│   ├── analyses/
│   ├── uploads/
│   └── uploads_index.json
├── models/
├── requirements.txt
├── Dockerfile
├── docker-compose.yml
├── README.md
└── HOW_TO_USE.md
```
